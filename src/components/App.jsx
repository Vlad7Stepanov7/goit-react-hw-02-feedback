import PropTypes from 'prop-types';
export const App = (props) => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101'
      }}
    >
      
    </div>
  );
};
App.protoType = {
   props: PropTypes.object
 }